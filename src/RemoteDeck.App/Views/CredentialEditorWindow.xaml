<ui:FluentWindow x:Class="RemoteDeck.App.Views.CredentialEditorWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:controls="clr-namespace:RemoteDeck.App.Controls"
                 xmlns:res="clr-namespace:RemoteDeck.App.Resources"
                 Title="{x:Static res:Strings.CredEditor_Title}" Width="440" SizeToContent="Height"
                 ExtendsContentIntoTitleBar="True" WindowBackdropType="Mica" WindowCornerPreference="Round"
                 WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
    <StackPanel Margin="0,0,0,12">
        <ui:TitleBar Title="{x:Static res:Strings.CredEditor_Title}" ShowMinimize="False" ShowMaximize="False" />
        <Grid Margin="16,4,16,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="{x:Static res:Strings.CredEditor_Label}" VerticalAlignment="Center" />
            <ui:TextBox Grid.Row="0" Grid.Column="1" x:Name="LabelInput" Margin="0,4" controls:Mark.Invalid="{Binding LabelInvalid}" Text="{Binding Label, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Grid.Row="1" Text="{x:Static res:Strings.CredEditor_UserName}" VerticalAlignment="Center" />
            <ui:TextBox Grid.Row="1" Grid.Column="1" Margin="0,4" controls:Mark.Invalid="{Binding UserNameInvalid}" Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Grid.Row="2" Text="{x:Static res:Strings.Common_Domain}" VerticalAlignment="Center" />
            <ui:TextBox Grid.Row="2" Grid.Column="1" Margin="0,4" PlaceholderText="{x:Static res:Strings.CredEditor_DomainPlaceholder}" Text="{Binding Domain, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Grid.Row="3" Text="{Binding PasswordLabel}" VerticalAlignment="Center" />
            <!-- Native PasswordBox on purpose: the only control that keeps the secret out of managed strings. -->
            <PasswordBox Grid.Row="3" Grid.Column="1" x:Name="PasswordInput" Margin="0,4"
                         controls:PasswordPlaceholder.Text="{Binding PasswordPlaceholder}" />
            <TextBlock Grid.Row="4" Grid.Column="1" Margin="0,0,0,4" Opacity="0.7" FontSize="{StaticResource RdFontMd}" Text="{x:Static res:Strings.Common_RequiredHint}" />
        </Grid>
        <ui:InfoBar x:Name="ErrorBar" Margin="16,8,16,0" Severity="Error" IsOpen="False" IsClosable="False"
                    Message="{Binding Errors}" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="16,12,16,0">
            <ui:Button Content="{x:Static res:Strings.Common_Cancel}" Margin="0,0,8,0" IsCancel="True" Click="OnCancel" />
            <ui:Button Content="{x:Static res:Strings.Common_Save}" Appearance="Primary" IsDefault="True" Click="OnSave" />
        </StackPanel>
    </StackPanel>
</ui:FluentWindow>
